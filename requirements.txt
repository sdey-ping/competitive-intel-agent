# ── AI Orchestration ──────────────────────────────────────────────────────────
langgraph>=0.2.0                # stream_mode="updates" requires 0.2+
langchain>=0.2.0
langchain-core>=0.2.0           # HumanMessage, SystemMessage (explicit dep)
langchain-anthropic>=0.3.0
anthropic>=0.40.0

# ── Web Scraping ──────────────────────────────────────────────────────────────
# NOTE: playwright removed — scraper_tool.py now uses requests + BeautifulSoup only
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0                     # fast HTML parser for BeautifulSoup

# ── Google Integration ────────────────────────────────────────────────────────
google-api-python-client>=2.100.0
google-auth>=2.0.0              # google.auth.transport.requests (gdrive_tool.py)
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.1.0

# ── YouTube ───────────────────────────────────────────────────────────────────
youtube-transcript-api>=0.6.0

# ── UI & Config ───────────────────────────────────────────────────────────────
streamlit>=1.32.0
python-dotenv>=1.0.0

# ── stdlib (no pip install needed) ───────────────────────────────────────────
# collections, urllib.parse, sqlite3, smtplib, json, time, re, os, io, base64
